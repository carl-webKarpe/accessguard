function toast(message, tone = '') { const wrap = document.querySelector('.toast-wrap') || Object.assign(document.body.appendChild(document.createElement('div')), { className:'toast-wrap' }); const item = document.createElement('div'); item.className = `toast ${tone}`; item.textContent = message; wrap.append(item); setTimeout(() => item.remove(), 3600); }
function showModal(title, content) { const backdrop = document.createElement('div'); backdrop.className = 'modal-backdrop'; backdrop.innerHTML = `<section class="modal"><div class="flex items-start justify-between gap-4"><h2 class="text-xl font-extrabold">${title}</h2><button aria-label="Close" class="close-modal text-xl text-slate-500">×</button></div><div class="mt-5">${content}</div></section>`; backdrop.onclick = event => { if (event.target === backdrop || event.target.closest('.close-modal')) backdrop.remove(); }; document.body.append(backdrop); return backdrop; }
function escapeHTML(value) { const element = document.createElement('div'); element.textContent = value || ''; return element.innerHTML; }
function formatDate(value) { return value ? new Intl.DateTimeFormat(undefined, { dateStyle:'medium', timeStyle:'short' }).format(new Date(value)) : 'Never'; }